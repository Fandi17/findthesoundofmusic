<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindTheSoundOfMusic"
        tools:targetApi="31">

        <!-- Aktivitas Splashscreen -->
        <activity
            android:name=".Splashscreenactivity"
            android:exported="true">
            <intent-filter>
                <!-- Menentukan aktivitas ini sebagai titik masuk aplikasi -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.lib_name"
                android:value="com.example.library_name"/> <!-- Sesuaikan jika diperlukan -->
        </activity>

        <!-- Aktivitas Utama -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- Aktivitas ini dapat diakses dari bagian lain aplikasi -->
        </activity>

    </application>

</manifest>
